# Turning Good Agent Design System

## 定位

Turning Good Agent 是高频使用的本机 Agent 工作台。界面应优先帮助用户完成任务、理解状态和控制风险，而不是展示功能或制造营销氛围。

设计关键词：克制、高级、安静、清晰、可持续使用。

## 核心原则

1. 一个页面只保留一个主工作面。聊天、编辑、排程或数据处理等主任务必须优先；配置、日志、观测和历史信息按需展示。
2. 功能结构可以不对称，但视觉系统必须一致。面板宽度、顶部节奏、留白、圆角和背景层级应形成统一规则。
3. 优先使用背景层级、留白和轻阴影分区。细边框只用于输入、代码、高风险操作或确有必要的可编辑边界。
4. 默认显示摘要，细节逐层展开。不要把日志、原始数据、配置或大量卡片同时推给用户。
5. 动画必须对应真实状态变化，例如尺寸、位置、可见性或任务进度；不得以内部淡入淡出伪装布局跳变。

## 视觉语言

- 使用大圆角控制和面板，避免生硬直角与过多不同圆角值。
- 建议只维护三档圆角：紧凑控件、面板、胶囊按钮。
- 深色主题应沉稳、低眩光；浅色主题应干净、低噪声。两者都需要独立校验，不能仅做颜色反转。
- 色彩只承担语义：主操作、成功、警告和危险。高饱和色不可作为大面积装饰。
- 阴影用于表达可操作性、悬浮层级和当前交互，不用于制造持续的视觉噪声。
- 不使用营销式渐变背景、装饰光球、Bento 卡片墙或大面积无意义插图。

## 布局与层级

- 主内容区应始终稳定、可读，不能被抽屉、滚动容器或浮层遮挡。
- 相邻侧栏或抽屉应使用一致的宽度策略与顶部基线；功能不同不意味着视觉重量失衡。
- 页面常态不依赖密集分割线。使用不同背景表面、内边距和留白形成层级。
- 长内容必须拥有独立、可预测的滚动区域，不能影响其他区域的滚动、尺寸或焦点。
- 所有固定尺寸控件应有稳定约束，避免状态变化导致布局跳动。

## 控件与交互

- 高频操作应紧凑、可预测、低摩擦；高风险操作应明确可见，但不污染普通操作的视觉层级。
- 悬停反馈只作用于用户实际指向的元素。避免父容器、子按钮和图标同时叠加阴影。
- 不为普通控件显示鼠标悬浮说明文字。图标仍需具备可访问名称、键盘焦点和合理的焦点顺序；只读状态读数可使用紧凑 Tooltip，内容必须与当前值直接相关。
- 不直接使用浏览器原生确认框、选择框、滚动条或突兀默认焦点样式；应使用产品内统一的菜单、对话框和焦点反馈。
- 菜单、弹层与抽屉必须支持点击外部关闭、正确层级和边界碰撞处理，不能被父容器裁切。
- 图标操作只适用于用户容易识别的高频功能；纯图标控件仍应保留无障碍语义。

## 输入与状态

- 输入区是主任务的一部分，应与主内容列对齐，并确保发送、停止、权限等操作始终完整可见。
- 输入框应支持多行内容，不应因长文本、滚动条或状态切换破坏按钮对齐。
- 与发送/停止并列的只读上下文占用环默认仅显示进度，悬浮或键盘聚焦时显示“已用 token / 最大 token、已用、剩余”三行居中读数；它不承担跳转或设置操作。
- 运行中状态只展示真实、可验证的步骤；不展示、推断或伪造模型内部思维链。
- 审批、失败、停止与成功应有明确的语义色和文字，但避免冗余解释。

## 信息展示

- 助手正文、用户输入、活动状态和观测信息应有清晰的视觉优先级。
- 用户短消息可紧凑包裹内容；正文内容优先保证阅读，而不是强行卡片化。
- 观测面板使用连续信息面表达摘要与分类；单条记录、原始 JSON、日志等更深内容仅在用户展开时显示。
- 代码、JSON 和长命令需要可复制、可滚动和可读的呈现方式。

## 验收方法

每次视觉修改均应在真实页面中检查：

- 深色与浅色主题。
- 长文本、空状态、加载、错误、审批、完成和停止状态。
- 菜单、抽屉、对话框的打开、关闭、遮挡和滚动。
- 主内容、输入区与侧栏在不同窗口宽度下的对齐与可见性。
- 悬停、键盘焦点和无障碍名称。

优先依据真实截图和真实交互判断质量，不以静态 CSS 或单一屏幕像素尺寸替代实际验证。
